//java 强类型语言
// const getArea = (w:number,h:number):number => {
//     return w * h;
// }
// getArea(5,6)

const getAddNumbers = (a: number, b: number): number => {
    return a + b;
}
getAddNumbers(4, 4.4);