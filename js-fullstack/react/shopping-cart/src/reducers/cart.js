const inintialState = {
  addedIds: [],
  quantityById:{}
}
const cart = (state = inintialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
export default cart;